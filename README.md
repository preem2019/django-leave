"# django-leave" 
ระบบ eLeave - ระบบขออนุญาตออกนอกสถานที่
eLeave เป็นระบบจัดการคำขออนุญาตออกนอกสถานที่สำหรับองค์กร (Web Application) ที่สร้างด้วย Django (Python) ถูกออกแบบมาเพื่อทดแทนกระบวนการยื่นเอกสารแบบเดิม ให้เป็นดิจิทัลทั้งหมด ตั้งแต่การยื่นคำขอ, การอนุมัติตามลำดับขั้น, จนถึงการบันทึกเวลาเข้า-ออกที่จุดรักษาความปลอดภัย
✨ คุณสมบัติหลัก (Features)
ระบบนี้แบ่งการทำงานตามบทบาทของผู้ใช้:

👤 1. พนักงาน (Employee)
ยื่นคำขออนุญาตออกนอกสถานที่ (ระบุวันที่, ระยะเวลา, เหตุผล)
แนบไฟล์เอกสารประกอบคำขอ
ติดตามสถานะคำขอ (รออนุมัติ, อนุมัติแล้ว, ถูกปฏิเสธ)
รับการแจ้งเตือนเมื่อสถานะเปลี่ยน (ผ่าน Email และ LINE)
ให้ข้อมูลเพิ่มเติม (Provide Info) หากผู้อนุมัติร้องขอ
ยกเลิก (Cancel) คำขอที่ยังไม่ถูกดำเนินการ
พิมพ์ (Print) ใบคำขอที่ได้รับอนุมัติแล้ว
แก้ไขข้อมูลส่วนตัว (เบอร์โทร, อีเมล, Line User ID) และเปลี่ยนรหัสผ่าน

👥 2. ผู้อนุมัติ (Manager / Supervisor)
รับ "กล่องงานอนุมัติ" (Approval Inbox)
ตรวจสอบรายละเอียดคำขอของผู้ใต้บังคับบัญชา
อนุมัติ (Approve), ปฏิเสธ (Reject), หรือ ขอข้อมูลเพิ่มเติม (Request Info)
ระบบอนุมัติแบบลำดับขั้น (Manager -> Supervisor -> HR/Safety)

👮 3. เจ้าหน้าที่รักษาความปลอดภัย (Security)
แดชบอร์ดสำหรับ รปภ. โดยเฉพาะ
แสดงรายการพนักงานที่ "พร้อมออก" (อนุมัติแล้ว แต่ยังไม่บันทึกเวลาออก)
บันทึกเวลาออก (Time Out) ของพนักงาน
แสดงรายการพนักงานที่ "อยู่ข้างนอก" (ออกไปแล้ว แต่ยังไม่กลับ)
บันทึกเวลากลับเข้า (Time In) พร้อมอัปโหลดรูปถ่าย (ถ้ามี)
ระบบบันทึกบุคคลภายนอก (Visitor Log) ทั้งขาเข้าและขาออก

⚙️ 4. ผู้ดูแลระบบ (HR / Admin)
จัดการข้อมูลพนักงาน (สร้าง, แก้ไข, ลบ)
กำหนดบทบาท (Role) และแผนก (Department) ให้พนักงาน
ดูรายงานสรุปสถิติ (Statistics Dashboard) ในรูปแบบกราฟ
ดูรายงานการเข้า-ออก (In-Out History Report) พร้อม Export เป็น Excel
ตั้งค่าการแสดงผลเว็บ (เช่น สีธีม, ชื่อแบรนด์, ข้อความ Footer)

🚀 วิธีการติดตั้ง (Installation)
ข้อกำหนดเบื้องต้น
Python 3.10+
Git
ฐานข้อมูล MySQL (หรือฐานข้อมูลอื่นที่ Django รองรับ)
ขั้นตอนการติดตั้ง
Clone Repository:
git clone [URL-ของ-REPOSITORY-ของคุณ]
cd [ชื่อโฟลเดอร์โปรเจกต์]


สร้างและเปิดใช้งาน Virtual Environment:
(Windows)
python -m venv venv
.\venv\Scripts\activate


(macOS / Linux)
python3 -m venv venv
source venv/bin/activate


ติดตั้ง Dependencies:
pip install -r requirements.txt


ตั้งค่าฐานข้อมูล (Database):
สร้างฐานข้อมูลเปล่าใน MySQL (เช่น eleave_db โดยตั้งค่า utf8mb4_general_ci)
สำคัญ: โปรเจกต์นี้ใช้ mysqlclient หากติดตั้งไม่ผ่านบน Windows อาจต้องดาวน์โหลด wheel มาติดตั้งเอง หรือติดตั้งผ่าน Visual C++ Build Tools
🔧 การตั้งค่า (Configuration) - (สำคัญมาก)
โปรเจกต์นี้ใช้ไฟล์ .gitignore เพื่อซ่อนไฟล์ที่มีข้อมูลความลับ คุณต้องสร้างไฟล์เหล่านี้ขึ้นมาเอง:
1. ไฟล์ site_config.json
(สร้างไฟล์นี้ไว้ที่ โฟลเดอร์หลัก ระดับเดียวกับ manage.py)
{
    "brand_name": "eLeave",
    "footer_text": "© 2025 ระบบขออนุญาตออกนอกสถานที่",
    "color_primary": "#00afdb",
    "color_success": "#198754",
    "color_warning": "#f39c12",
    "color_danger": "#e74c3c",
    "color_info": "#0dcaf0" 
}


2. ไฟล์ leave/settings.py
เนื่องจากไฟล์นี้ถูก gitignore ไว้ ให้คุณสร้างไฟล์ใหม่ชื่อ settings.py ภายในโฟลเดอร์ leave/ และคัดลอกโค้ดตัวอย่างข้างล่างนี้ไปวาง จากนั้นกรอกข้อมูลของคุณเอง:
from pathlib import Path
import os
from django.contrib.messages import constants as messages

BASE_DIR = Path(__file__).resolve().parent.parent

# ==============================================================================
# !!! กรอก SECRET_KEY ของคุณเอง !!!
# (คุณสามารถสร้าง Key ใหม่ได้จาก: [https://djecrety.ir/](https://djecrety.ir/))
# ==============================================================================
SECRET_KEY = 'YOUR_DJANGO_SECRET_KEY_GOES_HERE'

DEBUG = True  # เปลี่ยนเป็น False เมื่อใช้งานจริง
ALLOWED_HOSTS = [] # ใส่ Domain ของคุณเมื่อใช้งานจริง เช่น ['[www.myapp.com](https://www.myapp.com)']


# Application definition
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'app',
    'db_manager',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'leave.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [BASE_DIR / 'templates'],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                'app.context_processors.load_site_config', # โหลด config สี
            ],
        },
    },
]

WSGI_APPLICATION = 'leave.wsgi.application'

# ==============================================================================
# !!! ตั้งค่าฐานข้อมูลของคุณ !!!
# ==============================================================================
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'eleave_db',     # <-- ชื่อฐานข้อมูลที่คุณสร้าง
        'USER': 'root',          # <-- ชื่อผู้ใช้ MySQL
        'PASSWORD': '',          # <-- รหัสผ่าน MySQL
        'HOST': '127.0.0.1',     # 'localhost'
        'PORT': '3306',
        'OPTIONS': {
            'init_command': "SET sql_mode='STRICT_TRANS_TABLES'",
        },
    }
}

# Password validation
AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]

# Internationalization
LANGUAGE_CODE = 'th'
TIME_ZONE = 'Asia/Bangkok'
USE_I18N = True
USE_TZ = True

# Static files (CSS, JavaScript, Images)
STATIC_URL = 'static/'
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# Login/Logout URLs
LOGIN_URL = 'app:login'
LOGIN_REDIRECT_URL = 'app:dashboard'
LOGOUT_REDIRECT_URL = 'app:login'

# Bootstrap Message Tags
MESSAGE_TAGS = {
    messages.DEBUG: 'secondary',
    messages.INFO: 'info',
    messages.SUCCESS: 'success',
    messages.WARNING: 'warning',
    messages.ERROR: 'danger',
}

# ==============================================================================
# !!! ตั้งค่า Email และ LINE !!!
# ==============================================================================

# --- Email Settings ---
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com' # หรือ SMTP Host ของคุณ
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'your-email@gmail.com'     # <-- ใส่อีเมลของคุณ
EMAIL_HOST_PASSWORD = 'your-app-password'  # <-- ใส่ App Password (ไม่ใช่รหัสผ่าน Gmail)
DEFAULT_FROM_EMAIL = EMAIL_HOST_USER
ADMIN_EMAIL = 'your-email@gmail.com' # อีเมลสำหรับรับการแจ้งเตือนตอนทดสอบ

# --- Line Messaging API Settings ---
LINE_CHANNEL_ACCESS_TOKEN = 'YOUR_LINE_CHANNEL_ACCESS_TOKEN' # <-- ใส่ Token ของคุณ
LINE_TEST_USER_ID = 'YOUR_LINE_USER_ID' # <-- ใส่ User ID ของคุณสำหรับรับการแจ้งเตือนตอนทดสอบ


5. สร้างฐานข้อมูลและเริ่มระบบ
Migrate ฐานข้อมูล (สร้างตาราง):
python manage.py migrate


สร้าง Superuser (บัญชีผู้ดูแลสูงสุด):
python manage.py createsuperuser

(ตั้งชื่อ Username, Email, และ Password สำหรับ Admin)
รันเซิร์ฟเวอร์:
python manage.py runserver


เข้าสู่ระบบที่ http://127.0.0.1:8000/ ด้วยบัญชี Superuser ที่คุณเพิ่งสร้าง
📖 วิธีใช้งานระบบ (Quick Start)
เข้าสู่ระบบครั้งแรก (ด้วย Superuser):
ไปที่ http://127.0.0.1:8000/admin/
ไปที่ App -> Roles / Positions / Departments เพื่อเพิ่มข้อมูลพื้นฐาน (เช่น แผนก IT, ตำแหน่ง Staff, บทบาท Manager, HR, Security ฯลฯ)
หมายเหตุ: ชื่อ Role manager, supervisor, hr, safety, security เป็นชื่อเฉพาะที่ระบบใช้
ตั้งค่าเว็บ:
ไปที่ http://127.0.0.1:8000/settings/
ปรับแต่งสี, ชื่อแบรนด์, และ Footer ตามต้องการ
สร้างบัญชีพนักงาน (สำหรับ HR/Admin):
ไปที่ http://127.0.0.1:8000/users/
คลิก "เพิ่มพนักงานใหม่"
กรอกข้อมูล Login และกำหนด "บทบาท (Role)" ให้ถูกต้อง (สำคัญมาก)
ยื่นคำขอ (สำหรับ Employee):
ล็อกอินด้วยบัญชีพนักงานทั่วไป
ไปที่ "ยื่นคำขออนุญาต"
อนุมัติคำขอ (สำหรับ Manager/Supervisor):
ล็อกอินด้วยบัญชี Manager
ไปที่ "กล่องงานอนุมัติ"
บันทึกเวลา (สำหรับ Security):
ล็อกอินด้วยบัญชี Security
ระบบจะนำทางไปที่ "จุดตรวจสอบ รปภ." (/security/dashboard/) โดยอัตโนมัติ
